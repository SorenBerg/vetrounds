deployment:
  staging:
    branch: master
    commands:
      - ./script/heroku_deploy.sh vetrounds-staging:
          timeout: 300
  production:
    branch: production
    commands:
      - ./script/heroku_deploy.sh vetrounds:
          timeout: 300